
$(document).ready(function(){
	windWidth = $(window).innerWidth();
	$('.container-fluid').css('width', windWidth)

	jumbWidth = $('.jumbotron').innerWidth();
	$('#proj-box').css('width', jumbWidth);
	
});