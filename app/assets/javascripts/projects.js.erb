$(document).ready(function(){
	navHeight = $('#navbar').innerHeight();
	$('#proj-box').css('margin-top', navHeight);
	
	windHeight = $(window).innerHeight();
	$('#proj-box').css('height', windHeight);
	
});